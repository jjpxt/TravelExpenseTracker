<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TravelExpenseTracker.Pages.ExpensesCategoriesPage"
             BackgroundColor="WhiteSmoke"
             xmlns:vm="clr-namespace:TravelExpenseTracker.ViewModels"
             xmlns:model="clr-namespace:TravelExpenseTracker.Models"
             x:DataType="vm:ExpensesCategoriesViewModel"
             Shell.NavBarIsVisible="True"
             Title="Manage Expense Categories"
             >

    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="plus.svg" Command="{Binding AddCategoriesCommand}" />
    </ContentPage.ToolbarItems>
    
    <CollectionView ItemsSource="{Binding Categories}" Margin="15">

        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="4"/>
        </CollectionView.ItemsLayout>
        
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="model:ExpenseCategoryModel">
                <Border StrokeShape="RoundRectangle 4" BackgroundColor="White" StrokeThickness="0" Padding="10">
                    <Label Text="{Binding Name}" FontSize="18"/>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
    
</ContentPage>